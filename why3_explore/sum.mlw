module SumEquiv
  use int.Int

  let loop_sum (n: int) : int =
    let ref acc = 0 in
    for i = 0 to n do
        acc <- acc + i
    done;
    acc


  let direct_sum (n: int) : int = (n * (n + 1)) / 2
end
